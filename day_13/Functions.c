#include <stdio.h>
#include <stdlib.h>

// C Functions

/*
- A function is a block of code which only runs when it is called.
- You can pass data, known as parameters, into a function.
- Functions are used to perform certain actions, and they are important
  for reusing code: Define the code once, and use it many times.
*/

// Create a Function

/*
--> To create (often referred to as declare) your own function, specify
the name of the function, followed by parentheses () and curly brackets {}

*/

void myFunction() { // myFunction() is the name of the function
                    // void means that the function does not have a return value
	
  // code to be executed
      // Inside the function (the body), add code that defines what the function should do
}
